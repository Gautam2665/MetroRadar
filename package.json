{
  "name": "metroradar",
  "version": "0.1.0",
  "private": true,
  "description": "Urban Mobility Intelligence Platform",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "backend:dev": "npm run start:dev --workspace=apps/backend",
    "frontend:dev": "npm run dev --workspace=apps/frontend",
    "db:generate": "prisma generate --schema=database/prisma/schema.prisma",
    "db:push": "prisma db push --schema=database/prisma/schema.prisma",
    "db:seed": "npx tsx database/prisma/seed.ts",
    "certify:national": "npx tsx tools/certify-national-gtfs.ts",
    "enrich:geometry": "npx tsx tools/enrich-national-geometry.ts",
    "test:backend": "npm run test --workspace=apps/backend",
    "test:e2e": "npm run test:e2e --workspace=apps/backend",
    "lint": "npm run lint --workspaces --if-present",
    "build:backend": "npm run build --workspace=apps/backend",
    "build:frontend": "npm run build --workspace=apps/frontend",
    "build": "npm run build:backend && npm run build:frontend",
    "test": "npm run test --workspaces --if-present"
  },
  "prisma": {
    "seed": "npx tsx database/prisma/seed.ts"
  },
  "devDependencies": {
    "prisma": "^6.2.1",
    "ts-node": "^10.9.2",
    "tsx": "^4.19.2"
  },
  "dependencies": {
    "@prisma/client": "^6.2.1"
  },
  "optionalDependencies": {
    "lightningcss-linux-x64-gnu": "1.32.0",
    "lightningcss-linux-x64-musl": "1.32.0",
    "@tailwindcss/oxide-linux-x64-gnu": "4.3.2",
    "@tailwindcss/oxide-linux-x64-musl": "4.3.2"
  }
}
